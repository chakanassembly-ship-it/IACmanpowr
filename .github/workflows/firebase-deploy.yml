name: Deploy to Firebase

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18

<<<<<<< HEAD
      - name: Clean install dependencies
        run: |
          rm -rf node_modules package-lock.json
          npm install
=======
      - name: Install dependencies (clean + fix)
        run: |
          npm ci || npm install
          npm rebuild @tailwindcss/oxide --force || true
>>>>>>> 110ad5f (Fix Tailwind oxide CI issue)

      - name: Build project
        run: npm run build

      - name: Deploy to Firebase
        run: npx firebase-tools deploy --only hosting --token "$FIREBASE_TOKEN"
